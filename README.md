# WebSec Scanner

A **Linux-based web application security scanner** that helps identify common vulnerabilities in web applications.  
Supports **SQL Injection (SQLi)** detection, **Cross-Site Scripting (XSS)** and **Cross-Site Request Forgery (CSRF)**.(Not Finished Yet)

Since I do not have authorization to perform penetration tests on external websites, I did everything locally using DVWA.